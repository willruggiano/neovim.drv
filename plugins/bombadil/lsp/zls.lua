local config = {
  settings = {
    zls = {
      -- enable_build_on_save = true, -- default with a check step
      semantic_tokens = "full",
      -- warn_style = true,
    },
  },
} --[[@type vim.lsp.Config]]

return config
